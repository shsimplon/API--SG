// stockage de user id : on pourra le fournir à chaque fois sans repassé de server pour le demander à chaque fois
// : on le stocke avec huks context

import {createContext} from 'react';

export const UidContext = createContext();
 